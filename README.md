# Student Management System

This is a **React-based Student Management System** built with **Vite** and styled using **Tailwind CSS**. The system allows users to register, log in, register for courses, and view their registered courses.

## Features

- **User Registration**: Users can create an account with a username and password.
- **User Login**: Users can log in with their credentials to access the system.
- **Course Registration**: Users can register for courses.
- **My Courses**: Users can view the list of courses they have registered for.
- **Responsive Design**: The application is styled with Tailwind CSS for a modern and responsive UI.

## Technologies Used

- **React**: For building the user interface.
- **React Router**: For routing and navigation.
- **Vite**: For fast development and build tooling.
- **Tailwind CSS**: For styling the application.

## Installation

1. Clone the repository:

   ```bash
   git clone <repository-url>
   cd frontend-std
